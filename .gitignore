# --- Models & weights (fetched via scripts/download_models.sh, never committed) ---
*.names
*.cfg
*.weights
*.onnx
models/*
!models/README.md

# --- Build artifacts & compiled binaries ---
*.out
**/build/
# Extensionless Mach-O executables historically slipped past *.out; keep them out.
# (Contributors must build into build/, which is ignored above.)

# --- Captured / generated media ---
*.mp4
*.avi
*.jpg
*.jpeg
frame_*.jpg
temp_car_image.jpg
output_480p.avi

# --- Third-party heavy deps ---
*/libtorch

# --- Editor / OS ---
*.DS_Store
**/.vscode/

# --- Docs images kept intentionally ---
!docs/img/*.png
!docs/img/*.jpg

# Internal planning doc — not published
V2_UPGRADE_PLAN.md

# ALPR runtime event log
alpr_events.jsonl
